{
  "name": "@fallroot/jamo",
  "version": "0.1.0",
  "description": "A simple utility for hangul jamo",
  "scripts": {
    "build": "babel src/jamo.js --out-dir build",
    "test": "jest --coverage --watch test/*.js"
  },
  "keywords": ["hangul", "jamo", "korean"],
  "author": "Chookeun Moon <fallroot@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "@babel/cli": "^7.4.4",
    "@babel/core": "^7.4.5",
    "@babel/preset-env": "^7.4.5",
    "babel-eslint": "^10.0.2",
    "jest": "^24.8.0",
    "standard": "^12.0.1"
  },
  "babel": {
    "presets": [
      [
        "@babel/preset-env",
        {
          "modules": "umd"
        }
      ]
    ]
  },
  "standard": {
    "env": {
      "jest": true
    },
    "parser": "babel-eslint"
  }
}
